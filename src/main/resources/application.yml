spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/binance
    username: postgres
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
